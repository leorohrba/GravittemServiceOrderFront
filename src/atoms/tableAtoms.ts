import { atom } from 'jotai'

export const rowAtom = atom([])

export const rowKeyAtom = atom([])

export const tableDataAtom = atom([])

export const loadingTableDataAtom = atom(true)

export const columnsAtom = atom([])
